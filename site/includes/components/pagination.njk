{% if pagination.pages.length > 1 %}
    <nav class="mt-16"aria-labelledby="my-pagination">
        <h2 class="sr-only" id="my-pagination">Pagination</h2>
        <ol class="flex justify-center">
            {%- for pageEntry in pagination.pages %}
            <li class="mx-4"><a href="{{ pagination.hrefs[ loop.index0 ] }}"{% if page.url == pagination.hrefs[ loop.index0 ] %} class="border-solid border-black border-b" aria-current="page"{% endif %}>{{ loop.index }}</a></li>
            {%- endfor %}
        </ol>
    </nav>
{% endif %}